[project]
name = "copilot-codespaces-demo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["benchmarks"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-codspeed>=5.0.3",
]
